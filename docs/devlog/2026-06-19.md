# DevLog · 2026-06-19

> 格式约定：每条记「做了什么 / 为什么 / 踩了什么坑 / 下一步」。一天一文件。

## 做了什么
1. **重组 `docs/` 结构**：
   - 旧的 `docs/project/` 5 份文档（PRODUCT_VISION / PROJECT_CONTEXT / PROJECT_CONTEXT_FOR_CURSOR / AI_AGENT_WORKING_PROMPTS / SAAS_ROADMAP）→ 归档到 `docs/archive/2026-06-19/`。
   - 新建活文档目录：`docs/architecture/`、`docs/devlog/`，并加 `docs/INDEX.md` 作导航。
   - `docs/eval/`、`docs/images/` 保持不动（仍是活文档/资源）。
2. **写出 `docs/architecture/AGENT_PROTOCOL.md`（多智能体 v1 协议草案）**：定义 Planner / Answer Worker / Synthesizer 三个 Agent 的职责、输入、输出、失败处理；新增 4 个状态字段（+1 预留）；明确哪些旧契约不能动；给出 COMPARISON 完整状态流转示例。

## 为什么
- 旧文档「说一套、代码是另一套」，需要一个**以代码为准、按时间归档**的新体系，作为多智能体改造的约束（PRD）。
- 先定协议再写代码，避免改造跑偏 / 返工。

## 踩坑 / 发现（旧文档与代码不一致，已记录待修正）
1. `archive/2026-06-19/PROJECT_CONTEXT.md` 把「`@app.on_event('startup')` 待迁移到 lifespan」列为技术债——
   但 `app/main.py` **已经用 lifespan 了**，这条债其实已还。旧文档过时。
2. 同文档称「embedding 无缓存」。根目录确实有个 `.embedding_cache/embeddings.json`（~1MB），
   但 `app/` 里**没有任何代码引用它**（grep `cache/hashlib/pickle` 零命中）。
   → 结论：「当前代码不缓存 embedding」仍然成立，那个文件是个**没接上的遗留产物**。后续要么接上缓存、要么删掉它。

## 下一步
- [ ] 评审 `AGENT_PROTOCOL.md`，确认 Planner/Worker/Synthesizer 的输入输出 schema。
- [ ] 写 `architecture/ARCHITECTURE.md`（替代归档里的 PROJECT_CONTEXT.md）。
- [ ] 写 `eval/EVAL_PLAN.md`：定义单链路 vs 多智能体在 5 道 COMPARISON 题（q008/q009/q010/q019/q020）上的 A/B 口径。
- [ ] 协议确认后，再按 `AGENT_PROTOCOL.md §9.4` 的改动清单动代码。
